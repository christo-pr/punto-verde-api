"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ImageUserController');

describe('controllers:ImageUserController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
