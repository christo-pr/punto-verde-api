"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/NeighborhoodScrapController');

describe('controllers:NeighborhoodScrapController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
