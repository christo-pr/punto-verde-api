"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ScrapSectorController');

describe('controllers:ScrapSectorController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
