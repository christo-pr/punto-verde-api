"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ImageController');

describe('controllers:ImageController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
