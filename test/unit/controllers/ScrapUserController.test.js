"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ScrapUserController');

describe('controllers:ScrapUserController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
