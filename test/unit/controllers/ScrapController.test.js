"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ScrapController');

describe('controllers:ScrapController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
