"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/ScrapUser');
const Fixture = require('../../fixtures/ScrapUserFixture');

describe('models:ScrapUser', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
