"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/ScrapSector');
const Fixture = require('../../fixtures/ScrapSectorFixture');

describe('models:ScrapSector', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
