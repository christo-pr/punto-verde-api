"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/NeighborhoodScrap');
const Fixture = require('../../fixtures/NeighborhoodScrapFixture');

describe('models:NeighborhoodScrap', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
