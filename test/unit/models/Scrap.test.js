"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Scrap');
const Fixture = require('../../fixtures/ScrapFixture');

describe('models:Scrap', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
