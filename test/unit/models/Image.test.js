"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Image');
const Fixture = require('../../fixtures/ImagesFixture');

describe('models:Image', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
