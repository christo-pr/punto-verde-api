"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Neighborhood');
const Fixture = require('../../fixtures/NeighborhoodFixture');

describe('models:Neighborhood', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
